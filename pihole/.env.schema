# @import(../)
# ----------
# Set from the pihole app password in settings
# Note: If there's no password set, this still needs to be set. If this is the case, set it to a random string.
# @sensitive @required
PIHOLE_TOKEN=
